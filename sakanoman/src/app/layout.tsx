import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SakanOman - Find Your Perfect Home in Oman",
  description: "Discover apartments, rooms, and studios for rent in Muscat, Sohar, Salalah and across Oman. Connect directly with landlords and find your ideal accommodation.",
  keywords: "rent, apartment, room, studio, Oman, Muscat, Sohar, Salalah, accommodation, housing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
